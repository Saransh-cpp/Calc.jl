function multiply(a::Float64, b::Float64)
    """
    Perform multiplication between two floating point numbers.

    Parameters
    ----------
    a : Float64
        Number 1.
    b : Float64
        Number 2.

    Returns
    a * b : Float64
    """
    a * b
end
